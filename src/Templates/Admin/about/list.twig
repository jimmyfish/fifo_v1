{% extends 'Admin/Layouts/layouts.twig' %}

{% block content %}

    <!-- Team  -->
    <div class="row">
        <div class="col-md-12">
            <h1 class="page-header">Komunitas</h1>
                <a href="{{ path('admin_team_create') }}" class="btn btn-primary">Buat Baru</a>
            <p>&nbsp;</p>
        </div>
        <!-- /.col-lg-12 -->
    </div>
    {% for message in app.session.flashbag.get('team_success') %}
        <div class="alert alert-success alert-dismissable">
            <a href="#" class="close" data-dismiss="alert" aria-label="close">&times;</a>
            <strong>Success!</strong> {{ message }}
        </div>
    {% endfor %}
    {% for message in app.session.flashbag.get('team_error') %}
        <div class="alert alert-danger alert-dismissable">
            <a href="#" class="close" data-dismiss="alert" aria-label="close">&times;</a>
            <strong>Ups!</strong> {{ message }}
        </div>
    {% endfor %}
    {% for message in app.session.flashbag.get('team_edit') %}
        <div class="alert alert-danger alert-dismissable">
            <a href="#" class="close" data-dismiss="alert" aria-label="close">&times;</a>
            <strong>Success!</strong> {{ message }}
        </div>
    {% endfor %}
    <div class="row">
        <div class="col-lg-12">
            <div class="panel panel-default">
                <!-- /.panel-heading -->
                <div class="panel-body">
                    <table width="100%" class="table table-striped table-bordered table-hover">
                        <thead>
                        <tr>
                            <th>No</th>
                            <th>Judul</th>
                            <th>deskripsi</th>
                            <th>Gambar</th>
                            <th>Action</th>
                        </tr>
                        </thead>
                        <tbody>
                        {% set i = 1 %}
                        {% for info in teamList %}
                            <tr>
                                <td>{{ i }}</td>
                                <td>{{ info.title }}</td>
                                <td>{{ info.description }}</td>
                                <td>
                                    <a href="{{ info.images }}" target="_blank" class="btn btn-primary btn-block btn-xs">Buka</a>
                                </td>
                                <td>
                                    <a href="{{ path('admin_team_edit', {id:info.id}) }}">
                                        <i class="fa fa-pencil text-info"></i>
                                    </a>
                                    <a href="{{ path('admin_team_delete', {id:info.id}) }}">
                                        <i class="fa fa-trash text-danger"></i>
                                    </a>
                                </td>
                            </tr>

                            {% set i = i + 1 %}
                        {% endfor %}

                        </tbody>
                    </table>
                </div>
                <!-- /.panel-body -->
            </div>
            <!-- /.panel -->
        </div>
        <!-- /.col-lg-12 -->
    </div>

    <!-- Member  -->
    <div class="row">
        <div class="col-md-12">
            <h1 class="page-header">Anggota Komunitas</h1>
            <a href="{{ path('admin_member_create') }}" class="btn btn-primary">Buat Baru</a>
            <p>&nbsp;</p>
        </div>
        <!-- /.col-lg-12 -->
    </div>

    {% for message in app.session.flashbag.get('member_success') %}
        <div class="alert alert-success alert-dismissable">
            <a href="#" class="close" data-dismiss="alert" aria-label="close">&times;</a>
            <strong>Success!</strong> {{ message }}
        </div>
    {% endfor %}
    {% for message in app.session.flashbag.get('member_error') %}
        <div class="alert alert-danger alert-dismissable">
            <a href="#" class="close" data-dismiss="alert" aria-label="close">&times;</a>
            <strong>Ups!</strong> {{ message }}
        </div>
    {% endfor %}
    {% for message in app.session.flashbag.get('member_edit') %}
        <div class="alert alert-danger alert-dismissable">
            <a href="#" class="close" data-dismiss="alert" aria-label="close">&times;</a>
            <strong>Success!</strong> {{ message }}
        </div>
    {% endfor %}
    <div class="row">
        <div class="col-lg-12">
            <div class="panel panel-default">
                <!-- /.panel-heading -->
                <div class="panel-body">
                    <table width="100%" class="table table-striped table-bordered table-hover" id="list-member">
                        <thead>
                        <tr>
                            <th>No</th>
                            <th>Nama Lengkap</th>
                            <th>Jabatan</th>
                            <th>deskripsi</th>
                            <th>Facebook</th>
                            <th>Instagram</th>
                            <th>Gambar</th>
                            <th>Action</th>
                        </tr>
                        </thead>
                        <tbody>
                        {% set i = 1 %}
                        {% for info in member %}
                            <tr>
                                <td>{{ i }}</td>
                                <td>{{ info.title }}</td>
                                <td>{{ info.job }}</td>
                                <td>{{ info.description }}</td>
                                <td>{{ info.fbLink }}</td>
                                <td>{{ info.igLink }}</td>
                                <td>
                                    <span class="btn btn-danger btn-crud"><a href="">Check</a>
                                    </span>
                                </td>
                                <td>
                                    <a href="{{ path('admin_member_edit', {id:info.id}) }}">
                                        <i class="fa fa-pencil text-info"></i>
                                    </a>
                                    <a href="{{ path('admin_member_delete', {id:info.id}) }}">
                                        <i class="fa fa-trash text-danger"></i>
                                    </a>
                                </td>
                            </tr>

                            {% set i = i + 1 %}
                        {% endfor %}

                        </tbody>
                    </table>
                </div>
                <!-- /.panel-body -->
            </div>
            <!-- /.panel -->
        </div>
        <!-- /.col-lg-12 -->
    </div>

    <!-- Sponsorship  -->
    <div class="row">
        <div class="col-md-12">
            <h1 class="page-header">Sponsorship</h1>
            <a href="{{ path('admin_sponsor_create') }}" class="btn btn-primary">Buat Baru</a>
            <p>&nbsp;</p>
        </div>
        <!-- /.col-lg-12 -->
    </div>
    {% for message in app.session.flashbag.get('sponsor_success') %}
        <div class="alert alert-success alert-dismissable">
            <a href="#" class="close" data-dismiss="alert" aria-label="close">&times;</a>
            <strong>Success!</strong> {{ message }}
        </div>
    {% endfor %}
    {% for message in app.session.flashbag.get('sponsor_error') %}
        <div class="alert alert-danger alert-dismissable">
            <a href="#" class="close" data-dismiss="alert" aria-label="close">&times;</a>
            <strong>Ups!</strong> {{ message }}
        </div>
    {% endfor %}
    {% for message in app.session.flashbag.get('sponsor_edit') %}
        <div class="alert alert-danger alert-dismissable">
            <a href="#" class="close" data-dismiss="alert" aria-label="close">&times;</a>
            <strong>Success!</strong> {{ message }}
        </div>
    {% endfor %}
    <div class="row">
        <div class="col-lg-12">
            <div class="panel panel-default">
                <!-- /.panel-heading -->
                <div class="panel-body">
                    <table width="100%" class="table table-striped table-bordered table-hover" id="list-sponsor">
                        <thead>
                        <tr>
                            <th>No</th>
                            <th>Judul</th>
                            <th>deskripsi</th>
                            <th>No Telp/WA</th>
                            <th>Gambar</th>
                            <th>Action</th>
                        </tr>
                        </thead>
                        <tbody>
                        {% set i = 1 %}
                        {% for info in sponsor %}
                            <tr>
                                <td>{{ i }}</td>
                                <td>{{ info.title }}</td>
                                <td>{{ info.description }}</td>
                                <td>{{ info.telp }}</td>
                                <td>{{ info.images }}</td>
                                <td>
                                    <a href="{{ path('admin_sponsor_edit', {id:info.id}) }}">
                                        <i class="fa fa-pencil text-info"></i>
                                    </a>
                                    <a href="{{ path('admin_sponsor_delete', {id:info.id}) }}">
                                        <i class="fa fa-trash text-danger"></i>
                                    </a>
                                </td>
                            </tr>

                            {% set i = i + 1 %}
                        {% endfor %}

                        </tbody>
                    </table>
                </div>
                <!-- /.panel-body -->
            </div>
            <!-- /.panel -->
        </div>
        <!-- /.col-lg-12 -->
    </div>

{% endblock %}
{% block customJS %}
    <script>
    $(document).ready(function () {
        $('#list-member,#list-sponsor').DataTable({
            responsive: true,
            "columnDefs": [
                { "orderable": false, "targets": [4,3]},
                { "searchable": false, "targets": 4 }
            ]
        });

        
    });


    </script>
{% endblock %}